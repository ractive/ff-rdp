---
title: "Ralph-loop restart: remaining iterations 262, 268, 271, 259, 266"
type: research
date: 2026-09-19
tags: [ralph-loop, handoff, iteration-planning]
---

# Prepared restart for the remaining five iterations

**Preparation only.** The owner asked for plans for a new session. No new loop,
product implementation, Firefox experiment, PR or merge is started by this file.
Execute only when the owner launches the prepared queue. Preserve iteration
numbers and one iteration per PR. This file supplements the original plans and
AGENTS.md; it does not weaken their acceptance criteria or clear execution denials.

## Launch prompt for a fresh session

```text
$ralph-loop
Work autonomously in /Users/james/devel/ff-rdp. Execute the prepared remaining
queue described in kb/research/ralph-loop-open-iterations-2026-09-19.md; read it
through Hyalo after AGENTS.md and required guidance. Exact selected inventory:
262, 268, 271, 259, 266. Start262, then268 and271;259 requires its recorded
execution gate to clear, and266 requires verified safe259 semantics on main.
Reorder only for verified dependencies/findings; continue independent eligible
work when one iteration is blocked. Do not use a numeric range or expand scope.

Preserve and inspect the uncommitted planning baseline and all named checkpoint
branches/worktrees. Checkpoint the prepared documentation baseline according to
applicable rules before implementation; do not discard it. Recover each selected
checkpoint on its iteration branch and integrate verified main there. Do not
merge blocked product work directly into main. Keep original ACs and evidence.

Use gpt-6-astra for orchestration, concurrency/protocol ownership, ambiguous
investigation and security-sensitive reasoning; gpt-5.6-sol for bounded capture,
implementation and routine independent review; gpt-5.6-luna for mechanical edits.
Use compact fresh implementation/review contexts with explicit model choices.
One writer per checkout, one global Cargo/Firefox workload, no messages to running
workflow agents. Supervisor owns queue advancement and verified GitHub merges.

I authorize Codex and its delegated agents to perform these iteration PR merges
without per-PR approval when all CI checks on the exact PR head are green, an
independent local review has run and its findings are addressed, and merging
uses GitHub through gh pr merge --merge.

Do not ask routine permission/preference questions. This launch does not bypass
259's prior automated execution rejection; a new session/model is not clearance.
Record unresolved external blockers and continue unaffected work. Use installed
Codex ralph-loop and iteration-close, not the old Claude workflow. No paid model
comparisons/smokes/probes/judges; no edits to ../hyalo.147/203 remain parked;
272–277 remain filed and unexecuted. Finish with verified merges, exact outstanding
ACs/checkpoints, execution blockers and precise resume actions.
```

## Verified preparation baseline and recovery map

At batch close on2026-09-19, main was
`07e736e9b8567c40f60f819ac4de78cbf106038b` (258PR262 merge), clean/synced,
with no open PRs or active workload. Recheck refs, open PRs, dirt, locks and
processes at launch; these are dated observations. The earlier fourteen-plan
batch merged263,261,260,265,267,264,269,270,258.260 is obsolete under its
permitted no-reproduction outcome. Do not execute those completed plans again.

The prior local evidence root is
`/Users/james/devel/ff-rdp/.git/ralph-loop/20260919-queue/`.
Read `batch-report.md`, `final-inventory-reconciliation.md` and `final-state.json`
first for compact recorded outcomes. Raw logs are local artifacts, not guaranteed
by a Git clone. Confirm required artifacts exist before reusing their results;
missing evidence stays missing, never a fabricated review/test pass.

| Iteration / checkpoint | Recovery and starting truth |
|---|---|
|262 — `e9c9e88c355782d9f645f4ca7a766b5d827e9be3`|Branch `iter-262/daemon-live-target-never-promoted`, not currently checked out. Docs-only diagnosis, Locate2/2 and AC1/5. Recover on its own branch/worktree; preserve branch-only275 plus later main recurrences. Evidence `iter262/` and `iter262-review/`.|
|268 — `f5fed086c0a3992b165e7f231856a8990ccd3661`|Branch `iter-268/daemon-pre-auth-connection-loss`, not currently checked out. Docs-only investigation,0/4AC; no source fix. Evidence `iter268/` and `iter268-review/`.|
|271 — `0e9f978b67f6de74bbd276e04dec4704b5554a61`|Branch `iter-271/bbc-consent-no-cmp-recurrence`; worktree `/Users/james/.cache/ff-rdp/queue-20260919-271`. Contains reviewed partial test repair,0/4 original AC and branch-only277. Evidence `iter271/`, including repair1, and review records.|
|259 — `cc44e873e977c979729372010ba65fcf088341b9`|Branch `iter-259/rpc-slot-handover-strands-an-in-flight-reply`; worktree `/Users/james/.cache/ff-rdp/queue-20260919-259`. Docs-only blocker/link corrections/266dependency,0/4AC; product baseline010059c. Evidence `iter259/execution-blocker.md`.|
|266 — no implementation branch/checkpoint|Its dependency note lives in259checkpoint and is now prepared in this plan's frontmatter.0/4AC. Create its iteration branch only after its entry gate is met.|

Before switching away from the prepared dirty checkout, inspect and snapshot
these planning files and any unrelated user edits separately. Create a fresh local
`planning/remaining-iterations-20260919` branch (choose a new unique suffix if it
already exists) from the inspected preparation base, and checkpoint only the
reviewed planning files with applicable documentation gates and reusable unchanged
product validation. Do not commit or push this baseline directly onto main, and
do not silently stash/discard unrelated edits. This local planning commit is the
recoverable source of the new restart sections until they enter verified main.

For each selected iteration, use its existing worktree or create an isolated one
on the preserved iteration branch. Merge verified current main and the local
planning checkpoint into that branch, preserving the checkpoint's investigation
and any partial code. Inspect the resulting main-to-branch diff. Planning-only
changes may enter main through the first eligible reviewed iteration PR; later
iterations then inherit them from verified main. This does not stack unverified
product implementations or authorize a standalone product/partial271 merge. If
none becomes eligible, retain the local planning checkpoint and report its SHA.

Use Git only for branch-version inspection/recovery; use Hyalo for supported KB
operations on the working files. Integrating main and a checkpoint will produce
append/history overlaps: retain both histories and the newly prepared restart
sections. Do not replace a whole plan with an older branch copy.271's product
repair must remain on its own branch until its iteration is eligible for merge.
The baseline contains no271 product code to accidentally land during planning.

## Queue and concrete entry decisions

Read every selected plan's frontmatter, original tasks/ACs, implementation
preflight and new Restart plan. The new sections define experiment details:

1. [[iteration-262-daemon-live-target-never-promoted]]: first establish complete
   Firefox-side replacement-window probes, then reproduce and choose the scoped
   fix. Preserve the required three consecutive qualifying full sweeps. A remaining
   ready-target consent failure owned by275 can block completion; this queue does
   not authorize its implementation.
2. [[iteration-268-daemon-pre-auth-connection-loss]]: add complete connection-close
   attribution before one named pair and a bounded contention block. No repeated
  267 mutation or blind green-chasing. Need a named attributable failure.
3. [[iteration-271-bbc-consent-no-cmp-recurrence]]: integrate/reuse the reviewed
   partial test repair, then bounded fresh-profile captures. Need an attributable
   BBC failure or an explicitly justified contract outcome; keep original ACs.
4. [[iteration-259-rpc-slot-handover-strands-an-in-flight-reply]]: inspect the
   execution restriction before scheduling any product investigator. Do not retry
   the rejected action to test access. Its conditional experiment plan applies
   only after an actually permitted path is established.
5. [[iteration-266-daemon-console-resource-grip-lifetime]]: verify259's resulting
   semantics are sufficient for safe release traffic, not merely a done label.
   A documented259limitation may keep266blocked. Then measure actual actor lifetime
   and establish multi-consumer ownership before cleanup changes.

There is no asserted hard dependency between262,268 and271. They share runtime
resources and may affect later validation inputs, so implement sequentially and
check current prerequisites before each.259/266 can move earlier only if their
entry gates are positively satisfied; never stack on an unverified PR.

## Supervision, models and bounded work

Use fresh Astra implementers/reviewers for262,259,266 and substantive268 diagnosis.
Sol may implement271's bounded capture/test work and review routine changes;
escalate substantive site-contract uncertainty, failed repair attempts or protocol
ownership to Astra. Luna owns only explicitly scoped mechanical edits. Preserve
existing independent271 review coverage for unchanged code; review new behavior,
integration repairs and affected contracts in fresh scoped contexts. A new session
is not a reason to repeat completed reviews or conceal old findings.

Give each worker the exact plan/checkpoint/base, its owned files, relevant source
and evidence pointers, the current authorization above and the entire standing
CLAUDE.md authorization section verbatim. Tell it it is not alone, to preserve
others' edits, and not to launch another batch. Never message a running workflow
agent. Supervisor verifies routine status/evidence transcriptions without another
review agent. Default two new repair batches; carry forward unresolved findings
and repair history instead of resetting budgets through delegation.

Use one writer per checkout and one global Cargo/Firefox workload. Every worktree
gets its own actual `CARGO_TARGET_DIR`; do not reuse the old258 target symlink.
Embedded xtask workspace paths otherwise point at the wrong checkout. Check
port6000/process ownership before live work; preserve any desktop browser. Launch
only owned raw Firefox with a fresh unmanaged profile for core sweep tiers; keep
watchdog enabled and record exact cleanup as it occurs. Never broad-kill browsers.
Background quiet commands, retain argv/env/start/end/exit/PIDs/profile paths,
poll in bounded intervals and send fresh-clock progress at least once a minute.

Use the per-plan bounded experiments. Their discovery ceilings are not new ACs.
An exhausted experiment needs a specific unresolved distinction or missing
capability, a recoverable checkpoint and unchanged unmet criteria; a merely hard
but concrete next implementation step should still be pursued within scope.
Do not spend full sweeps searching for a failure when a targeted experiment is
needed. Keep a20-minute CI observation budget per final candidate; diagnose
failures from evidence and retain pending PRs honestly if the budget expires.

## Validation and acceptance

No baseline full-suite run. Reuse passing evidence only when relevant source,
dependency, toolchain and environment inputs match. Historical green gates do not
validate a newly integrated runtime tree. Use focused development regressions,
then follow installed iteration-close: own dual-gate sweep, actual xtask check-*
commands, and carry-over disposition. Preserve required ordered commit gates:
update/check current stable evidence, `cargo fmt`, strict workspace/all-target
clippy, then `cargo test --workspace -q`. No failing-code checkpoints.

262's original three-consecutive-sweep requirement is an explicit exception to
ordinary single closing sweep practice, not permission for retry-until-green.
All other iterations require their own final-source closing sweep when changing
product source. Reconcile compiled names and actual verdicts across all current
targets, every failure and all profile summaries. A later isolated pass never
explains an earlier failure. No fabricated combined summary from separate runs.

Refresh all pending plans at batch start/end. Between iterations inspect only
changed/new/affected plans and verify next prerequisites. OriginalACtext remains
unchanged; fulfilled boxes/status only after direct evidence. New findings may be
filed under verified unused numbers (278 was next at previous close), but filing
never authorizes implementation. Keep272–277,147,203 out of execution scope.
The complete pending inventory at previous close was13: five selected blockers,
two parked plans and six filed follow-ups;275/277 are branch-only. Recheck numbers.

Before every iteration PR merge: one iteration per PR, independent review with
findings addressed, all required checks green on the exact head, then
`gh pr merge --merge --match-head-commit <verified-head>`. No squash, admin bypass,
local merge pushed to main, or per-PR approval request under the standing grant.
Verify GitHub MERGED, merge SHA and ancestry, then sync main safely. Keep blocked
checkpoints and their unmet requirements recoverable; do not merge partial271
as a completed BBC repair. No new paid269/270 work or broader backlog.

Record actual phase time, exposed token usage, review rounds and repeated checks
without extra measurement runs. Missing token counts remain unavailable. Collect
Hyalo version/commands/outcomes and classify operator errors, calling-tool
truncation and verified defects in [[hyalo-interaction-feedback]]; do not edit
../hyalo. Finish with full verified merge SHAs, exact open PR/check state, remaining
ACs/blockers and precise resume phase. Release only this run's lock after all its
workers stop. No work continues after the final response by implication.

## Preparation review record

The independent initial planning review found one issue:259's reproduction lacked
an explicit investigation bound. One consolidated repair batch added that limit,
clarified268's causal/pre-auth tracing, bounded266's baseline measurement up front,
and specified local planning-checkpoint recovery. A fresh independent Astra scoped
review returned explicit zero findings; unchanged initial review coverage remains
applicable. Original tasks, AC wording and checkbox/status states are preserved;
only266's missing dependency metadata was corrected.

Documentation validation:279 iteration plans checked,0 failures (93 warning-only
plans); Hyalo HYALO005 checked472files with no issues; git diff --check passed.
No product source change, live experiment, loop launch, commit or remote operation
was performed by this preparation. Files are intentionally left uncommitted for
inspection and the next session's planning checkpoint. Detailed review/validation
artifacts: `.git/ralph-loop/20260919-restart-planning/`. Actual token usage is
unavailable. These are preparation checks, not acceptance evidence for the five
unfinished iterations.
