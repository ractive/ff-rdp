{
  "symptom_hint": "Login claims success but no session cookie is set; subsequent API calls behave as unauthenticated.",
  "expected_diagnosis": "edge/proxy",
  "expected_evidence_commands": [
    "ff-rdp cookies",
    "ff-rdp click '#submit' --wait-for-network '/api/login'",
    "ff-rdp network --filter '/api/login' --detail --headers --limit 5",
    "ff-rdp cookies"
  ],
  "must_not_conclude": ["A2", "B5", "D2"]
}
